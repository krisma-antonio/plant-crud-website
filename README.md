# CRUD Website Plant Database
A simple CRUD web application that allows users to maintain database of plants. Created using Python, Flask, and sqlite database

To run. use commands:
$ export FLASK_APP=backend
$ flask run
